/**
 * ISI Consulting — isiConfig.js (Block 3C Section 5)
 * Future API endpoint config stubs. Set endpoints without touching pages.
 */
const isiConfig = {
  crmEndpoint: null,
  webhookEndpoint: null,
  cloudEndpoint: null
};

if (typeof window !== "undefined") {
  window.isiConfig = isiConfig;
}
